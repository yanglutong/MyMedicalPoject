<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="RememberPswdCheckboxTheme" parent="@android:style/Widget.CompoundButton.CheckBox">
        <item name="android:button">@drawable/checkbox_style1</item>
    </style>
    <style name="TextAppearanceHome" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">14sp</item>
    </style>
    <style name="Address1CheckboxTheme" parent="@android:style/Widget.CompoundButton.CheckBox">
        <item name="android:button">@drawable/checkbox_style4</item>
    </style>
    <!--底部导航RadioGroup-->
    <style name="main_bottom_tab_style">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_weight">1</item>
        <item name="android:button">@null</item>
        <item name="android:gravity">center</item>
        <item name="android:drawablePadding">6dp</item>
        <item name="android:paddingTop">8dp</item>
        <item name="android:paddingBottom">3dp</item>
        <item name="android:textColor">@color/rb_color</item>
        <item name="android:textSize">11sp</item>
    </style>
    <style name="MyRatingBar" parent="@android:style/Widget.RatingBar">
        <item name="android:progressDrawable">@drawable/ratingbar</item>
        <item name="android:maxHeight">25dp</item>
        <item name="android:minHeight">25dp</item>
    </style>

    <!--上拉刷新下拉加载的自定义刷新效果-->
    <style name="RefreshStyle" >
        <!-- 下拉状态的图标设置 -->
        <item name="srlDrawableArrow">@drawable/shequ</item>
        <!-- 释放状态的图标设置-->
        <item name="srlDrawableProgress">@drawable/shoucang</item>
        <!-- 下拉状态图标尺寸大小-->
        <item name="srlDrawableArrowSize">24dp</item>
        <!-- 释放状态图标尺寸大小-->
        <item name="srlDrawableProgressSize">24dp</item>
        <!-- 文字颜色 -->
        <item name="srlAccentColor">@color/txt_red</item>
        <!-- 上拉加载提示文字-->
        <item name="srlTextLoading">客官莫急</item>
        <!-- 上下拉状态提示文字设置-->
        <item name="srlTextPulling">下拉进行刷新</item>
        <!-- 松开刷新的文字设置-->
        <item name="srlTextRelease">松开刷新</item>
        <!-- 正在刷新中的状态文字设置-->
        <item name="srlTextRefreshing">加载中...</item>
        <!-- 刷新成功文字设置-->
        <item name="srlTextFinish">数据刷新成功</item>
    </style>
    <style name="AddressCheckboxTheme" parent="@android:style/Widget.CompoundButton.CheckBox">
        <item name="android:button">@drawable/checkbox_style3</item>
    </style>
</resources>